node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
.vite
*.log
.DS_Store

# Stray tsc emit — source is .ts/.tsx; tsconfig noEmit guards this, ignore as backstop.
# (root postcss.config.js / tailwind.config.js are genuine and tracked — not matched here)
/src/**/*.js
/vite.config.js
/vite.config.d.ts
*.tsbuildinfo

# Agent scratch git-worktrees (backend AUD-14 #370). Nested repos: an
# over-broad `git add` records them as mode-160000 gitlinks with no
# `.gitmodules` — pointers a fresh clone cannot resolve. The backend repo
# was hit; guarded here by the Stray-gitlink CI step.
.claude/worktrees/
